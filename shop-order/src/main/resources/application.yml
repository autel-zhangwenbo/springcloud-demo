server:
  port: 8091
spring:
  cloud:
    sentinel:
      transport:
        port: 8719                         # 跟控制台交流的端口,随意指定一个未使用的端口即可
        dashboard: 10.250.68.222:8829      # 指定控制台服务的地址
        #dashboard: entinel-dashboard.knative.private-cloud.autelrobotics.com      # 指定控制台服务的地址
    nacos:
      discovery:
        server-addr: 10.250.68.222:8848
  application:
    name: service-order
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.250.68.222:3306/shop?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: ecommerce
    password: a2460c22-a657-4f03-8cca-9756ebe225db
  jpa:
    hibernate:
      ddl-auto:
        auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
